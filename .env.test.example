# Neon Postgres test database (https://console.neon.tech)
# Must be a separate database from dev to avoid data conflicts.
DATABASE_URL=postgresql://user:password@host/testdb?sslmode=require

# E2E test user — create in Clerk dashboard, then set credentials here.
E2E_CLERK_USER_EMAIL=
E2E_CLERK_USER_PASSWORD=
